{
  "name": "{{.NodeID}}",
  "version": "0.1.0",
  "private": true,
  "description": "{{.ProjectName}} — Service: {{.NodeID}}. Generated by Acthur for node:fastify.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "node --watch src/index.js",
    "build": "npm ci --omit=dev",
    "start": "node src/index.js",
    "test": "node --test src/**/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "fastify": "^4.28.1",
    "@fastify/cors": "^9.0.1",
    "@fastify/compress": "^7.0.3"
  }
}
