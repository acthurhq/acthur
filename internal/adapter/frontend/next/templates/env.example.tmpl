# .env.example — Generated by Acthur for ui:next
# Copy to .env and fill in your values. Never commit .env.

APP_ENV=development
APP_PORT=3000

# Base URL of the backend API this frontend calls (set by Acthur from the
# backend node this frontend has a data_flow edge to). NEXT_PUBLIC_* vars
# are inlined into the client bundle at build time by Next.js.
NEXT_PUBLIC_API_URL=http://localhost:8080
