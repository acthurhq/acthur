// next.config.mjs — Generated by Acthur for ui:next node "{{.NodeID}}".
// Do not hand-edit; re-scaffold to pick up adapter changes.
//
// output: 'standalone' traces only the dependencies this app actually
// needs into .next/standalone, producing a self-contained server.js the
// same way go:fiber produces a single static binary — the Dockerfile
// copies just that output, not the whole node_modules tree.
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  reactStrictMode: true,
};

export default nextConfig;
