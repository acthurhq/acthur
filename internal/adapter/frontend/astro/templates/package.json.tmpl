{
  "name": "{{.NodeID}}",
  "version": "0.1.0",
  "private": true,
  "description": "{{.ProjectName}} — Service: {{.NodeID}}. Generated by Acthur for ui:astro.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "start": "node ./dist/server/entry.mjs",
    "test": "echo \"no tests configured yet\" && exit 0"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "@astrojs/node": "^8.3.4"
  }
}
