# .env.example — Generated by Acthur for ui:astro
# Copy to .env and fill in your values. Never commit .env.

APP_ENV=development
APP_PORT=4321

# Base URL of the backend API this frontend calls (set by Acthur from the
# backend node this frontend has a data_flow edge to).
PUBLIC_API_URL=http://localhost:8080
