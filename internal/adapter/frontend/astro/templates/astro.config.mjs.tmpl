// astro.config.mjs — Generated by Acthur for ui:astro node "{{.NodeID}}".
// Do not hand-edit; re-scaffold to pick up adapter changes.
//
// output: 'server' + the @astrojs/node adapter in standalone mode means
// this app serves its own HTTP server (dev and production alike) rather
// than emitting static files for another server to host — the same shape
// as every backend adapter, so the kernel's GET /health contract and the
// dev proxy work identically regardless of adapter.
import { defineConfig } from 'astro/config';
import node from '@astrojs/node';

export default defineConfig({
  output: 'server',
  adapter: node({ mode: 'standalone' }),
  server: {
    // The dev engine injects PORT (and APP_PORT) into this process's env
    // before spawning it — read here rather than hardcoded so the node's
    // acthur.yml port config is honored. @astrojs/node's standalone runtime
    // server reads process.env.PORT itself at boot, independent of this
    // config — kept in sync deliberately.
    port: Number(process.env.PORT) || 4321,
    host: true,
  },
});
