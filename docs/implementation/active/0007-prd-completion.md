# Implementation: PRD Completion — audit-gap closure (Phases 3/4/6/8 leftovers + Phase 9)

## Goal
Close every gap `docs/audit.md` (2026-07-06) identified between the PRD and the implementation, in dependency order: Phase 3 integration (doctor-in-dev, watcher wiring, DNS scoping, service commands), Phase 4 (importers, response validation), Phase 6/8 leftovers (db subcommands, acthur test, acthur build), then Phase 9 (new/init wizard, adapters, generate surfaces, secrets/flag/monitor, mcp/agent, deploy targets, ecosystem plugins). Every stub `notImplemented(N)` either gains a real implementation or is explicitly descoped in docs with the PRD updated.

## Owning Docs
- `docs/audit.md` (gap inventory) · `docs/acthur-prd.md` §21 · tracker issue #53

## Status
In-flight
- 2026-07-06 — main — Audit triaged: Phase 8 findings were stale (deploy wired + witnessed in c9e41ce/824fe9c; suite green), rest confirmed. Stale tracked `acthur` binary removed. Wave 1 delegated to parallel Sonnet worktree agents.
- 2026-07-06 — worktree agent-a0808583ee16135c5 (#56) — Phase 6/8 db+build+test gaps closed: `acthur db migrate:create` (shares `migrations.CreateNext` numbering with `db create`), `acthur db seed` (+ `--file`, lexical `seeds/*.sql` order, via injectable `SeedExecutor`), `acthur db reset` (`--yes`-gated down-all+up-all+seed via injectable `ResetSteps`), `acthur db studio` (adminer container via the `deploy.Runner` seam, prints URL), `acthur test [service]` and `acthur build` (both reuse `buildableServiceNodes` from the deploy gate and the gate's new `deploy.GoStream` seam) were all already implemented by the prior agent but never committed. Root cause: an unanchored `acthur` line in `.gitignore` (from the binary-untracking cleanup) matched the `cmd/acthur/` directory itself, so `git add -A` silently excluded `build.go`/`db.go`/`test.go` from the WIP checkpoint commit. Fixed by anchoring the rule to `/acthur` (repo-root binary only) and recovered the three files. Added missing unit tests (build_test.go, test_test.go, db_test.go) covering node selection, failure aggregation, studio target parsing, and runner lifecycle. `go build ./... && go vet ./... && go test ./...` all green. No `notImplemented(6)`/`notImplemented(8)` remains for these five surfaces. Phase 3/4/9 items in this doc are out of this slice's scope and untouched.
- 2026-07-06 — worktree agent-aa02558092e19d708 (issue #55, Phase 4 gaps) — Completed both items:
  1. Contract importers: salvaged and finished the in-flight `internal/contract/import_openapi.go` (fixed a missing `plain bool` arg at 3 call sites, otherwise didn't compile). Added `internal/contract/import_proto.go` (hand-rolled proto3 parser: messages → Types, service rpcs → Endpoints with flattened request/response fields) and `internal/contract/import_graphql.go` (hand-rolled GraphQL SDL parser: object types → Types, enums tracked for `enum(...)` field rendering, Query/Mutation/Subscription fields → Endpoints). No new deps — stdlib + existing `gopkg.in/yaml.v3` only. Fixtures under `testdata/contract-import/{openapi,proto,graphql}/`. Replaced the old "not yet supported" tests in `internal/contract/contract_test.go` with real round-trip import assertions. Lossy/conventional mappings (GraphQL list returns → `{"items": "array($Type)"}`, gRPC endpoints have no Method/Path in the HTTP sense) are documented in code comments in the importer files.
  2. Proxy response validation: `internal/proxy/proxy.go` now validates flow-route backend responses against the contract's Output schema via the existing `contract.Validator.ValidateResponse` (already present, just unused by the proxy). Matched contract/endpoint is threaded from request-time enforcement to `ModifyResponse` via request context (`flowCheckCtxKey`). Dev mode logs a warning and forwards unchanged; `--strict` rewrites the response into a structured 502 (`{"error":{"code":"response_contract_violation",...}}`) before it reaches the client — including fixing the stale `Content-Length` header that would otherwise truncate/hang the rewritten body. `cmd/acthur/commands.go` dev command help text and `--strict` flag description updated to mention 502 response blocking. Tests added to `internal/proxy/flow_test.go` mirroring the existing request-validation test shape.
  - Also fixed an unrelated but critical bug found mid-task per parent-session heads-up: `.gitignore`'s unanchored `acthur` line was silently excluding all new files under `cmd/acthur/` from `git add -A` (this is how the prior agent on this issue lost work) — anchored to `/acthur`.
  - Full suite (`go build ./... && go vet ./... && go test ./...`) green at handoff.
  - Deferred/out of scope: nothing from issue #55's stated scope. Not addressed here (belongs to other phases per audit): Phase 3 doctor/DNS/watcher integration, Phase 6/8/9 gaps.
- 2026-07-06 — worktree agent-aaad08f6a74ac851d (#54) — Phase 3 gaps closed:
  doctor preflight in `acthur dev` + `--skip-doctor` (salvaged from prior WIP,
  verified green); `internal/dns` DNS preflight for `*.acthur.local`-style dev
  domains (`--write-hosts` opt-in, never runs sudo); watcher wired into
  `DevEngine` via a new `adapter.SelfReloader` capability so file changes
  restart a node's process unless its adapter (e.g. go:fiber's air)
  self-reloads; and `acthur service add/logs/restart/health` implemented —
  logs/pidfiles persisted under `.acthur/` by the dev engine
  (`internal/process/control.go`), `restart` signals the recorded pid
  (SIGTERM, relying on the existing Supervisor auto-restart-on-crash path —
  no new daemon/RPC), `health` probes directly, `add` mirrors `add.go`'s
  YAML-edit-with-rollback pattern. All four gaps are unit-tested (fake
  process manager / fake DNS lookup / fake health poller — no real
  processes or network); `go build ./... && go vet ./... && go test ./...`
  green. Also fixed an unrelated repo-wide bug found while resuming: an
  unanchored `acthur` line in `.gitignore` was silently excluding new files
  under `cmd/acthur/` from `git add -A` (root-caused the prior agent's lost
  `dev_preflight_test.go`) — anchored to `/acthur` and recovered the file.
  Needs a live witness: this closes the gaps at the unit-test level only —
  no run against a real `acthur dev` process (real file-change restart,
  real DNS resolution, real `acthur service restart` against a live pid)
  has been performed in this worktree.

- 2026-07-06 — worktree agent-aa30aa5cfbf4bf9de (issue #58, Phase 9 go:chi + go:gin) — Added two new backend adapters at Scaffold/Run/Dockerize parity with go:fiber: `internal/adapter/backend/chi` (net/http + go-chi/chi/v5 + go-chi/cors) and `internal/adapter/backend/gin` (gin-gonic/gin + gin-contrib/cors). Each scaffolds the same project shape as go:fiber — `/health` + `/api/health` with the identical JSON contract, a zap structured-logger middleware, config/errors/ids packages (ULID or UUID-v4 per `IDStrategy`), air-based hot reload (`SelfReloads() == true`, same `.air.toml` rerun/rerun_delay behavior as the #33 go:fiber fix), and a `dockerfile.prod.tmpl` mirrored from go:fiber's hard-won details verbatim (build root package `.`, not `./...`; healthcheck against `127.0.0.1`, never `localhost`; EXPOSE/HEALTHCHECK omitted for portless nodes; non-root `acthur` user). Both register via blank import in `cmd/acthur/commands.go` next to go:fiber, so `acthur adapter list` picks them up automatically (verified live: both show `[Scaffold Run Dockerize]`). Framework-idiomatic adaptations where fiber's API has no direct net/http/gin equivalent: chi has no centralized `ErrorHandler`, so `internal/errors.Write(w, err)` is the per-handler seam; gin gets an `errors.Handler()` middleware that reads `c.Errors` after `c.Next()` since gin's error model is accumulate-then-inspect rather than return-a-value. `internal/adapter/adapter_test.go` extended with a parametrized `backendAdapterCases()` suite covering both adapters in parallel with the existing go:fiber tests (registration, Detect true/false/no-go.mod, Runnable+SelfReloader, Scaffold file set + health routes + air rerun config, module-path consistency, ID strategy selection, Dockerizable multi-stage/non-root/EXPOSE/HEALTHCHECK/portless/deterministic, exact capability set {Scaffold, Run, Dockerize}), plus `TestBackendAdapters_Scaffold_CompilesWithGoBuild`: scaffolds each into a temp dir and runs `go mod tidy && go build ./... && go vet ./...` — both generated go:chi and go:gin projects compile and vet clean. `go build ./... && go vet ./... && go test ./...` green across the whole repo. Not in scope for this slice (per issue #58's stated scope, and confirmed against `cmd/acthur/add.go` / `internal/generate`): the contract-driven code generator (`acthur add`, `acthur generate from-contract`) and the auth/rbac/multitenancy plugin generators remain go:fiber-only (`SupportedAdapters() []string{"go:fiber"}`) — extending those to chi/gin is a separate, larger effort not implied by this issue's "Scaffold/Run/Connectable/Dockerize parity" wording (which lists the adapter capability set, not that go:fiber itself implements Connectable — it doesn't, and neither do chi/gin).

- 2026-07-06 — main — Wave 1 merged to dev (all three branches), full suite green. Live witness (scratchpad witness-w1, gofiber api + postgres db): `acthur build` → static binary at `.acthur/build/api`; `acthur test` green; doctor preflight correctly aborted dev on missing tools (and the witness caught a real bug: `cmdVersion` used `cmd.Output()` but air prints its version to stderr — doctor reported installed air as missing; fixed with `CombinedOutput` + regression test); DNS preflight printed copy-pastable /etc/hosts instructions; `.acthur/logs/<node>.log` + `.acthur/run/<node>.pid` written by the engine; `service health` ✓, `service logs` tails live air/request output, `service restart` bounced the app (uptime reset, healthy after); file-change hot reload proven both ways — broken file → air build failure + shutdown + supervisor recovery, fixed file → rebuild → healthy, with zero engine-driven restarts (SelfReloader honored). Wave 1 acceptance criteria met; Wave 2 (Phase 9, #57–#65) launching.
- 2026-07-06 — worktree agent-abe2e6029e4ffa560 (#57, Phase 9 slice) — Completed all three gaps:
  1. `acthur new <project>` — non-interactive (flag-driven: `--adapter`, `--db`, `--module`) and interactive (injectable `io.Reader`/`io.Writer` wizard, prompts only for answers not already set via flags) paths share `resolveNewOptions` (`cmd/acthur/new.go`). Non-TTY stdin with any answer missing is a pointed error naming exactly which flags to pass — never a hang on an unsatisfiable read (detected via `github.com/mattn/go-isatty`, already an indirect dep, promoted to direct via `go mod tidy`). `scaffoldProject` writes a minimal valid `acthur.yml` (one `api` service node on the chosen backend adapter, optional `db` infra node on `db:postgres`, `api→proxy proxied_through` edge always present so the always-materialized kernel proxy node satisfies the no-orphan-node rule, `api→db depends_on` when `--db`), scaffolds the adapter's files under `<project>/api/` via its `Scaffolder.Scaffold` (same `scaffold.ResolveScaffoldContext` used by `acthur add`), and runs `git init` (best-effort — reported via `scaffoldResult.GitInit`, never fails the command). Adapter choice is validated against `adapter.ByCategory(CategoryBackend)` adapters that implement `Scaffolder` — picking e.g. `db:postgres` (registered but not a Scaffolder) is a pointed error.
  2. `acthur init` — same wizard/scaffold path targeting cwd instead of a fresh subdirectory; shares `scaffoldProject`'s existing-`acthur.yml` guard for the non-destructive guarantee.
  3. `acthur plugin remove <name>` — `cmd/acthur/plugin_remove.go` mirrors `add.go`'s YAML-edit-with-rollback pattern: line-based removal of the `  - name: <name>` entry and its nested fields (indented 4+ spaces, e.g. a `config:` block) from acthur.yml's `plugins:` list, reload + `graph.Build` to confirm the file is still valid (rolling back on any failure), pointed "not installed" error listing every currently-installed plugin name when the target isn't present. Fixed the same trailing-newline-loss bug `service add` had already been patched for (removing the last entry in the file could drop the file's final newline) — regression-tested.
  Live-witnessed with a built binary (not `go run`, since a fresh dev dir has no go.mod): `acthur new demo --adapter go:fiber --db --module github.com/acme/demo` → real `acthur.yml` + `demo/api/{main.go,go.mod,Dockerfile,...}` + `.git/`, and `acthur graph validate` against the generated project reports "all 3 nodes and 2 edges are valid" with zero errors; `acthur init` into an existing dir refuses a second run once `acthur.yml` exists; `acthur add migrations` → `acthur plugin remove migrations` round-tripped cleanly, second `remove` call correctly points at "no plugins listed"; `.gitignore`'s `/acthur` anchoring (fixed by prior agents) confirmed working — `git status` shows all four new files (`new.go`, `new_test.go`, `plugin_remove.go`, `plugin_remove_test.go`) as untracked, not silently excluded.
  Deferred/out of scope (per issue #57's stated scope — full interactive wizard steps like framework choice beyond adapter, plugin selection, deployment target selection, and `acthur init`'s stack-detection/adoption-of-existing-code scan are broader Phase 9 PRD ambitions not asked for here; only `go:fiber` is a registered backend Scaffolder today so the adapter prompt has exactly one real choice): `acthur init`'s file-detection/reconciliation phases (§7.2 Init Phases 1/4 "Scan"/"Reconcile" in the PRD) are not implemented — it only ever writes a fresh scaffold, it does not detect an existing non-Acthur codebase and adopt it non-destructively around existing files. `go build ./... && go vet ./... && go test ./...` all green at handoff.
- 2026-07-06 — worktree agent-a625fae97484cea44 (#59, Phase 9) — Backend adapter parity: added `rust:axum` and `node:fastify`, same capability shape as `go:fiber` (Scaffolder, Runnable, SelfReloader, Dockerizable — neither is Containerized, matching go:fiber's precedent of building from project source rather than an upstream image). Both scaffolds share go:fiber's behavioral contract: `/health` + `/api/health` (dev-proxy-prefixed) returning the identical `{status, service, uptime_seconds, checks}` JSON shape, structured request logging (tower-http `TraceLayer`/tracing for axum, Fastify's built-in pino logger), config from env with the same required-var set (APP_ENV/APP_PORT/APP_SECRET[generate]/DATABASE_URL/REDIS_URL), CORS + compression middleware, graceful shutdown on SIGTERM/SIGINT, ULID/UUID `IDStrategy` branching (axum: `ulid`/`uuid` crates; fastify: hand-rolled Crockford-base32 ULID — no external dep — plus `crypto.randomUUID`). Hot reload: `cargo-watch -x run` for axum, `node --watch src/index.js` for fastify — both self-restart the process themselves, so `SelfReloads() → true` on both (dev engine's file watcher defers to them, same as go:fiber's air). Dockerfile capability: scaffold-time `Dockerfile` (no healthcheck, hardcoded :8080, mirrors go:fiber's) plus `DockerfileFor` (Dockerizable, re-rendered per `acthur deploy`) — both multi-stage (rust:1.82-alpine musl builder → alpine:3.20 runtime; node:24-alpine builder → node:24-alpine runtime), non-root user, HEALTHCHECK against **127.0.0.1** (never `localhost` — confirmed alpine/musl resolves it to `::1`, same hard-won lesson as go:fiber's dockerfile.prod.tmpl), EXPOSE/HEALTHCHECK omitted when `ctx.Port == 0` (portless/worker nodes). Registered both in `cmd/acthur/commands.go` alongside go:fiber/db:postgres — `internal/deploy/artifacts` needed no changes since it projects any `Dockerizable` adapter generically via `adapter.Resolve` + type assertion; `acthur adapter list` confirms both show up with `[Scaffold Run Dockerize]`. Existing generator plugins (auth/rbac/multitenancy) remain go:fiber-only by explicit design (`SupportedAdapters()` gate) — out of this issue's scope, not a gap.
  - Verification: rebuilt this worktree onto current `dev` first (it had drifted ~40 commits stale, predating `docs/audit.md`, the Dockerizable capability, and issue #53-56's work — clean rebase, no conflicts). `cargo check` passes on a rendered rust:axum scaffold (13s cold, network access to crates.io confirmed available in this sandbox despite `curl` to crates.io returning 403 — cargo's own client works fine). Every rendered node:fastify `.js` file passes `node --check`; `package.json` parses as valid JSON with `fastify` declared and dev/build/test scripts present (real `npm install` skipped per scope — no dependency-shape or type-checking issue expected, plain JS was chosen specifically to avoid needing a TS compile step here).
  - Test coverage per adapter (`internal/adapter/backend/{rustaxum,fastify}/*_test.go`, ~30 tests each): registry/name/category/detect, env vars, Runnable commands + SelfReloader, Scaffold file production/content/ID-strategy branching, Dockerizable multi-stage/non-root/port-EXPOSE/127.0.0.1-healthcheck/no-port-omission/determinism, exact `CapabilitiesOf` set, plus the compile/syntax-check tracer-bullet test (gated behind `testing.Short()` like go:fiber's).
  - `go build ./... && go vet ./... && go test ./...` green (full repo, all packages) at handoff.
  - Deferred/out of scope: no live witness of `acthur dev` actually running a scaffolded rust:axum or node:fastify service end-to-end (no `cargo run`/full `npm install` performed — network-heavy and out of this slice's verification scope per task instructions). Generator plugin parity (auth/rbac/multitenancy for the new adapters) not attempted — those are explicitly go:fiber-only by design today and would be a separate, larger effort.
- 2026-07-06 — worktree agent-abe6004a9981b4288 (#60, Phase 9 frontend adapters) — Added `ui:astro` and `ui:next` at `internal/adapter/frontend/{astro,next}`, both `adapter.CategoryFrontend`, registered in `cmd/acthur/commands.go` (`acthur adapter list` now shows a `frontend` category, both `[Scaffold Run Dockerize]` — same capability shape as every backend adapter; neither is Connectable/Containerized, matching precedent). Key design decision: the kernel's health checker treats every service node uniformly (`internal/health/health.go` `strategyFor`: `node.IsService()` → HTTP GET /health, no category branch), so both frontends had to be real SSR/server apps with a live `/health` endpoint, not static sites — `ui:astro` uses `output: 'server'` + `@astrojs/node` in standalone mode (`src/pages/health.js` endpoint, `prerender = false`), `ui:next` uses the App Router (`app/health/route.ts`, Node.js runtime by default) with `output: 'standalone'` for a self-contained production server. Both emit the identical `{status, service, uptime_seconds, checks}` JSON body every backend adapter uses, so the kernel's health poller stays framework-agnostic. Port handling: the dev engine injects `PORT`/`APP_PORT` into the adapter's env before spawning it (`internal/engine/dev.go` `buildEnv`) — astro.config.mjs reads `process.env.PORT` directly (also the var @astrojs/node's own standalone runtime reads, so dev and prod agree); ui:next's `DevCommand` reads `APP_PORT`/`PORT` from the env map passed in and threads it as an explicit `next dev -- -p <port>` CLI arg for version-independent certainty. Both self-reload (`SelfReloads() → true`) via their own dev servers (astro's Vite-powered watcher, Next's Fast Refresh) — the dev engine's file watcher defers to them, same as every other adapter. Dockerfiles: multi-stage `node:24-alpine`, non-root `USER acthur`, `HEALTHCHECK` against `127.0.0.1` (never `localhost` — same hard-won alpine/musl lesson as every other adapter), `EXPOSE`/`HEALTHCHECK` omitted for portless nodes. `pathPrefixFor` in `internal/proxy/proxy.go` already special-cased `"web"`/`"frontend"`/`"app"`/`"www"` node IDs to the root `/` prefix (pre-existing, comment already mentioned "Next.js HMR") — no proxy changes needed, and no `/api/health` duplicate route was needed (that pattern in gofiber/chi/gin exists specifically for API services proxied under a non-root `/api-style` prefix; a root-mounted frontend doesn't need it).
  - Verification (all real, not mocked): rebuilt this worktree onto current `dev` first (brought in go:chi/go:gin/rust:axum/node:fastify/acthur-new-init and the backendAdapterCases suite). Per-adapter Go test suites (`internal/adapter/frontend/{astro,next}/*_test.go`, ~30 tests each, mirroring node:fastify's shape) cover registration/name/category, Detect true/false/no-package.json, EnvVars (present, no DATABASE_URL/APP_SECRET — frontends aren't backends), Runnable+SelfReloader, Scaffold file set + package.json validity + framework-specific config assertions (astro: `output: 'server'`, reads `process.env.PORT`; next: `output: 'standalone'`), health-endpoint contract assertions, Dockerizable multi-stage/non-root/port-EXPOSE/127.0.0.1-healthcheck/no-port-omission/determinism, exact `CapabilitiesOf` set. Beyond unit tests, did a full live witness for both, time-boxed but real: scaffolded each into `/tmp/.../scratchpad/witness-{astro,next}`, ran real `npm install` (349 packages for astro in ~11s, 28 for next in ~10s — network confirmed available) and real `npm run build` (astro: `astro build` produced `dist/server/entry.mjs`; next: `next build` — including full TypeScript type-check of `layout.tsx`/`page.tsx`/`health/route.ts` — produced `.next/standalone/server.js`), then **ran the actual built servers** (`node dist/server/entry.mjs` / `node .next/standalone/server.js`) with `PORT` env injected exactly as the dev engine would, and `curl`'d `/health`: both returned HTTP 200 with `{"status":"healthy","service":"web","uptime_seconds":...,"checks":{}}`, and `/` served 200. This is the strongest verification available short of wiring a live `acthur dev` run (not attempted — `acthur new`/`add` don't yet offer a frontend-adapter prompt path, an `acthur new` UX gap, not an adapter-contract gap; out of this issue's scope). Every generated `.js`/`.mjs` file passed `node --check`; `package.json`/`tsconfig.json` parsed as valid JSON. `go build ./... && go vet ./... && go test ./...` green across the whole repo at handoff.
  - Deferred/out of scope: no wiring of `ui:astro`/`ui:next` into `acthur new`'s adapter-choice prompt (today only scans `adapter.ByCategory(CategoryBackend)` Scaffolders — a frontend option there is a separate, `new.go`-level UX change, not part of this issue's "scaffold + dev-run wiring through the graph/proxy" scope). No generator-plugin parity (auth/rbac/multitenancy remain go:fiber-only by explicit design, same precedent as #58/#59). No live `acthur dev` process run against either adapter (see verification note above for why the strongest available alternative was performed instead).
- 2026-07-06 — worktree agent-a8bfa09c63ee47726 (#63, Phase 9 — `acthur mcp serve` + `acthur agent`) — Closed both remaining `notImplemented(9)` stubs named in the issue:
  1. **`acthur mcp serve`** — a real MCP server, not a mock: `internal/mcp` hand-rolls the wire protocol (JSON-RPC 2.0, newline-delimited over stdio per the MCP stdio-transport spec — one message per line, no embedded newlines) with `encoding/json` + `bufio.Scanner`/`io.Writer`, no SDK dependency. Implements `initialize`/`notifications/initialized`, `tools/list`, `tools/call`, and `ping`; a tool-handler error is reported as `isError: true` content, never a JSON-RPC-level error (per spec, a failed tool call is still a successful RPC). Five read-only tools, matching the issue's suggested set: `graph_query` (nodes/edges, optional type filter), `contract_list`, `contract_show` (latest or pinned version), `service_health` (live HTTP probe via the existing `internal/health.Checker`, injected through a small `HealthPoller` interface so mcp doesn't import health directly), `project_info` (node/adapter/plugin/contract counts + whether an AI provider is configured — never the key itself). `cmd/acthur/mcp.go`'s `loadMCPContext` is a deliberately non-fatal, non-plugin-loading sibling of the existing `loadGraph()`: `mcp serve` is long-lived and re-running `loadPlugins` per request would re-register every plugin hook/command on process-global state, so MCP tools see declared graph topology only, not plugin-injected nodes — documented as a known scoping tradeoff, not a silent gap. Tests drive the server over real JSON-RPC message text through `Server.Serve(io.Reader, io.Writer)` (in-memory pipes), including a `cmd/acthur/mcp_test.go` end-to-end session (`initialize` → `notifications/initialized` → `tools/list` → two `tools/call`s) against a real fixture `acthur.yml`.
  2. **`acthur agent`** (`explain`/`generate`/`diagnose`/`review`/`document`) — real, env-gated AI provider integration, not a stub returning fake text: `internal/aiagent` hand-rolls a client for the Anthropic Messages API (`net/http`, no SDK dep — same "no new deps" bar as mcp) behind a `Provider` interface; `ResolveProvider` reads acthur.yml's `ai:` block (`${VAR}` expansion via `os.ExpandEnv` plus a conventional-env-var fallback per provider) and returns a pointed `ErrProviderNotConfigured` or an unimplemented-provider error rather than ever faking a response — only `claude` is wired to a real call today; `openai`/`groq`/`ollama` are explicitly descoped in code comments and this note (PRD §17.7 lists all four; implementing three more real HTTP clients was judged out of this slice — a natural next issue). Every subcommand shares `runAgentTask` (`cmd/acthur/agent.go`): load live graph/contract context via the same `loadMCPContext`, build an LLM system prompt via `aiagent.BuildContext` (graph nodes/edges/contracts/plugins — never api_key), send the task-specific user prompt, print the response. `generate` is scoped in its own system-prompt instruction and in code comments to *proposing a plan*, not writing files — an LLM given unsupervised file-write access is not something this slice can honestly guarantee is safe, so PRD §17.6's "generate a feature with full graph context" is honored as structured-plan output for the operator to apply, explicitly not as autonomous codegen. `review` shells out to the real `git diff` (injectable `gitDiff` seam) and skips the API call entirely — printing a message instead — when the diff is empty, so a no-op review costs nothing. `document` reads a file (64KB cap) or lists a directory's immediate entries. `update-contract` from PRD §17.6 was not implemented — the shipped stub set in `commands.go` (predating this issue) only ever declared explain/generate/diagnose/review/document, and the issue's own scope line names only those five; `update-contract` is a PRD gap, not a stub this issue closed, and is left for a future issue.
  - Provider and git-diff are both injectable (`agentDeps`), so every test in `cmd/acthur/agent_test.go` and `internal/aiagent/*_test.go` runs against a fake `Provider` / `httptest.Server` — no real network call in the suite.
  - **Live witness (real, not simulated):** built the binary and ran `acthur mcp serve` against a real fixture project, piping a real multi-message JSON-RPC session (`initialize` → `notifications/initialized` → `tools/list` → `tools/call graph_query`) through stdin and inspecting the newline-delimited JSON on stdout — all four responses well-formed, `tools/list` enumerated all 5 tools, `graph_query` returned the real `api`/`db`/`proxy` nodes and the `depends_on` edge. Ran `acthur agent explain "..."` with no `ai:` block configured and got the exact pointed `ErrProviderNotConfigured` message on stderr with exit code 1 (not a crash, not a silent no-op). Then configured `ai: {provider: claude, api_key: ${ANTHROPIC_API_KEY}, base_url: http://127.0.0.1:8934}` pointing at a throwaway local Python HTTP server that asserts the real Anthropic wire shape (`x-api-key`, `anthropic-version: 2023-06-01` headers; `model`/`max_tokens`/`system`/`messages` body) and returns a Messages-API-shaped response — `acthur agent explain` made the real HTTP round trip and printed the fake server's answer verbatim, confirming the whole request/response path (header construction, JSON encoding, response parsing, error branch) works end-to-end, not just against `httptest` in-process fakes.
  - `go build ./... && go vet ./... && go test ./...` green across the whole repo at handoff. No new dependencies added to `go.mod` (both the MCP protocol and the Claude API client are hand-rolled per the task's explicit preference).
  - Deferred/out of scope: `openai`/`groq`/`ollama` providers (real HTTP clients for each — separate, larger effort per provider); `acthur agent update-contract` (not in the shipped stub set or this issue's scope, but present in PRD §17.6 — a documented PRD gap); wiring plugin-injected graph nodes into the MCP/agent context (would require re-architecting `loadMCPContext` to load plugins safely in a long-lived process without re-registering hooks per request); no live end-to-end test against the *real* `api.anthropic.com` (requires a funded API key — the local-fake-server witness above is the strongest verification available without one).

- 2026-07-06 — worktree agent-ad610227ce25b2ab8 (#62, Phase 9 secrets + flag + monitor) — Closed all three remaining `notImplemented(9)` command groups:
  1. `acthur secrets set/get/list/rm/rotate` — new `internal/secrets.Store` persists project-scoped, user-managed secrets under `.acthur/secrets/kv/<KEY>` as plaintext 0600 files (the "local .env, dev-only" model the PRD documents for the built-in secrets provider — not a vault, and documented as such in code and `--help`). Deliberately a separate directory from `internal/engine`'s pre-existing per-(nodeID,key) auto-generated adapter secrets (`.acthur/secrets/<node>.<key>`, unmodified) so the two never collide on disk. `secrets set` reads the value from stdin when omitted or passed `-`, keeping it out of shell history. `rotate` refuses to invent a new secret (errors if the key was never set) and generates a fresh `crypto/rand` value via the same `RandomValue` helper `Rotate` uses.
  2. `acthur flag create/list/enable/disable/toggle/retire` — new `internal/flags.Store` implements the PRD's local provider verbatim: `.acthur/flags.json`, hot-reloaded (every read re-parses from disk, so a flag toggled from a second CLI invocation is picked up by a running `acthur dev` without a restart). `EnvKey`/`EnvValue` give the `ACTHUR_FLAG_<NAME>` convention.
  3. `acthur monitor [--watch]` — new `cmd/acthur/monitor.go` builds a status table (node/type/adapter/health/PID) by polling every non-kernel node once via the same `health.Checker.Poll` `acthur service health` already uses, and reading `.acthur/run/<node>.pid` — no new daemon/RPC, same separate-process-reads-durable-state pattern as `acthur service logs/health/restart`. `--watch` refreshes every 2s until Ctrl+C, clearing the screen between refreshes.
  - Wired both new stores into the dev engine (`internal/engine/dev.go`): `DevEngine.buildEnv` now merges every project secret into a node's env (deferring to any engine-owned or adapter/edge-injected key of the same name) and exposes every feature flag as `ACTHUR_FLAG_<NAME>=true|false`, unconditionally. Both seams are nil (no-op) when `cfg.RootDir == ""`, so every pre-existing unit test constructing a bare `DevEngine` is unaffected.
  - Wired the secrets store into the pre-deploy gate (`internal/deploy/gate.go`): `GateInput.ResolveSecret` is an optional fallback consulted for a `RequiredEnv` var not already exported into the deploying shell — process env is always checked first and wins (CI/production deploy environments are unaffected), the fallback only makes local `acthur deploy` dev-loop testing convenient. `cmd/acthur/deploy.go` wires it to `secrets.New(root).Get`.
  - Live-witnessed with a built binary against a real scaffolded project (`acthur new witness-secrets --adapter go:fiber --db`): `secrets set/get/list` round-tripped (0600 confirmed via `stat`), stdin-piped `set` worked, `rotate` produced a new value, `rm` then `get` correctly errored "not set"; `flag create/list/enable/toggle/disable/retire` round-tripped, `.acthur/flags.json` matched expected JSON shape; `monitor` printed a real one-shot table (both nodes correctly reported unhealthy — nothing running) and `--watch` refreshed 3× over 5s at the 2s interval with visible screen-clear escapes.
  - Deferred/out of scope (per this issue's stated scope): `secrets rotate`'s "+ restart affected services" (PRD §19.10) only prints a pointed hint (`acthur service restart <name>`) rather than automatically tracking which nodes consume a given secret key and restarting them — no such dependency tracking exists anywhere else in the codebase yet, and inventing it was judged out of scope for this slice. `flag rollout --percentage` (PRD §37.1) is not implemented — only binary enable/disable/toggle; the `Flag` struct has no percentage field. Real secret encryption (age/NaCl, mentioned as an option in the parent task) was not implemented — the PRD's own secrets-plugin description explicitly calls the local provider ".env (dev only)" with no encryption, so plaintext+0600 (matching `internal/engine`'s existing precedent) was the correct reading, not a shortcut.
  - `go build ./... && go vet ./... && go test ./...` green at handoff (one `internal/process` test, `TestProcess_OutputCapture`, is flaky under full-suite parallel load — passes standalone and on rerun; untouched by this change, pre-existing).
- 2026-07-06 — worktree agent-a066ed7da785f18cc (#61, Phase 9) — Closed all five `notImplemented(9)` stubs this issue names: `acthur generate ai-context`, `generate ci`, `generate docs`, `generate skill <name>`, and `acthur graph visualize`. New packages, one per surface, each a pure renderer over cfg/graph/contracts returning `[]plugin.GeneratedFile` (or a string for visualize) with deterministic (sorted) output, persisted through the existing `internal/generate.WriteFiles(root, "", files)` write engine — root-relative paths like `.github/workflows/acthur.yml` route correctly with `nodeID=""` since `lockKey` only special-cases `migrations/`/`deploy/` and falls through to `filepath.Join("", path)` = `path` otherwise, so no changes were needed to the write engine itself:
  1. `internal/generate/aicontext` — `--tool claude` (default, `CLAUDE.md`) or `--tool cursor` (`.cursorrules`); renders nodes/adapters/edges/contracts/plugins tables from cfg+graph+`contract.LoadDir(root)`. Unsupported tool names error listing the two supported ones (acthur-prd.md §17.2 lists more tools — Cursor/Copilot/Windsurf/etc — only Claude+Cursor implemented here, additive to extend later).
  2. `internal/generate/ci` — `--target github-actions` (the only implemented target; `gitlab-ci`/`circleci` are named in the PRD but rejected with a pointed "not yet implemented" error rather than a stub). Workflow built via typed Go structs (`ghWorkflow`/`ghJob`/`ghStep`) marshaled with `yaml.v3` and round-tripped through `yaml.Unmarshal` inside `Generate` itself as a self-check before ever reaching disk — checkout, `actions/setup-go@v5` with `go-version: "1.22"` + cache, install acthur via the documented `curl -fsSL https://install.acthur.dev | sh` (acthur-prd.md's own install channel), then `acthur graph validate` / `acthur build` / `acthur test`.
  3. `internal/generate/docsgen` — one `docs/api/<name>.md` per contract (endpoints w/ method+path+auth+roles+rate-limit+input/output field tables+errors, events, types) plus a `docs/api/README.md` index table linking all of them, from `contract.Registry.All()`.
  4. `internal/generate/skillgen` — `.claude/skills/<name>/SKILL.md` with real frontmatter (`name`/`description`) matching every skill already on disk under `.claude/skills/*`, kebab-case name validated, body derived from the actual service adapters + active plugins (not lorem ipsum) — `Overwrite: false` so a user's filled-in skill is never clobbered by a legacy-path rewrite.
  5. `internal/generate/visualize` — `Mermaid(g)`/`Dot(g)`, dispatched via `Render(g, format)`; node shape by kind (service=box, infra=cylinder), edges labeled with type + contract names; wired to `acthur graph visualize [--format mermaid|dot] [--output file]` (stdout when `--output` is omitted).
  Live-witnessed with a built binary against `acthur new` scaffold + the vetangle `users.contract.yml` fixture: all five commands ran for real, `acthur graph validate` still passes afterward, generated `.github/workflows/acthur.yml` round-trips through Python's `yaml.safe_load` as a well-formed Actions workflow, and — this caught a real bug — the first `graph visualize` output showed a broken `\\n` in every node label (labels were built with a literal `\n` line-break escape then re-quoted through `fmt`'s `%q`, which re-escapes the existing backslash); fixed by dropping the embedded line break for a single-line label, then re-verified by rendering the fixed `.mmd` output through `@mermaid-js/mermaid-cli` to a real SVG (checked the SVG text contains every node/edge label) — the mermaid diagram actually renders, not just parses. Regression tests added for the no-double-escape invariant. `go build ./... && go vet ./... && go test ./...` green at handoff.
  Deferred/out of scope (per issue #61's stated scope): `gitlab-ci`/`circleci` CI targets and `nextra`/`readme`/`openapi` docs targets are named in acthur-prd.md but rejected with a clear error, not implemented; ai-context tools beyond Claude/Cursor (Copilot, Windsurf, Continue.dev, Aider, Zed) likewise; `.acthur/ai.lock` multi-tool tracking (acthur-prd.md §17.2) not implemented — every run overwrites the one file for the chosen tool via the normal `generated.lock` mechanism instead of a separate ai-specific lock. `acthur mcp serve`, `agent`, remaining deploy targets, secrets/flag/monitor, and ecosystem plugins remain `notImplemented(9)` — untouched, not this issue's scope.

- 2026-07-06 — worktree agent-aeb5f2f5bd72240ea (#64, Phase 9 deploy targets fly/railway/render) — Added three new deploy targets under `internal/deploy/{fly,railway,render}`, mirroring coolify's structure (self-contained package: `client.go` typed client + `target.go` implementing that package's own `DeployContext` interface + fake-HTTP-verified `*_test.go`), wired into `cmd/acthur/deploy.go` runDeploy's target switch, `requiredEnvVars`, and per-provider adapter context types (`flyContext`/`railwayContext`/`renderContext`, one each since Go can't have one type implement three interfaces differing only in `Services() []T`'s return type).
  - **Fly** (`internal/deploy/fly`): real Machines API (`https://api.machines.dev/v1`, Bearer auth) — `EnsureApp`, `EnsureMachine` (create-or-update by name), `WaitStarted` polling machine state. Build/push: the Machines API only runs already-built images, so `Target.Deploy` builds+pushes each service's Dockerfile to Fly's own registry (`registry.fly.io/<app>`) via the injected `deploy.Runner` (same seam `ComposeTarget` already uses for `docker compose`) — `docker login -u x -p <FLY_API_TOKEN>` is Fly's own documented registry-auth convention, so no external registry account is needed, unlike railway/render. Token: `FLY_API_TOKEN`.
  - **Railway** (`internal/deploy/railway`): real public GraphQL API (`https://backboard.railway.com/graphql/v2`, Bearer auth) — `EnsureProject`, `Project` (read services/environments), `EnsureService` (create-or-update via `serviceCreate`/`serviceInstanceUpdate` with an image source), `Deploy` (`serviceInstanceDeploy`), `LatestDeploymentStatus` polling to SUCCESS/FAILED/CRASHED. Railway's schema deploys from a source (repo or already-pullable image) — no raw-Dockerfile-upload endpoint exists — so this target requires an external registry the caller already has push+pull-by-Railway access to, via `RAILWAY_IMAGE_REGISTRY` (documented in the package doc as a hard requirement, not a default guess). Token: `RAILWAY_TOKEN`.
  - **Render** (`internal/deploy/render`): real REST API (`https://api.render.com/v1`, Bearer auth) — `EnsureOwner` (resolves the workspace, taking the first entry for a single-workspace token — documented limitation), `EnsureService` (list-by-name then `POST /v1/services` with `image.imagePath`), `Deploy` (`POST .../deploys` with `imageUrl`), `DeployStatus` polling to live/build_failed/update_failed/canceled/deactivated. Same registry requirement as railway (`RENDER_IMAGE_REGISTRY`) since Render also has no registry of its own and (per this issue's scope) uses REST service-create rather than a connected-repo/render.yaml blueprint flow. Key: `RENDER_API_KEY`.
  - `config.DeployTarget` already declared `TargetFly`/`TargetRailway`/`TargetRender` (prior work) — no config whitelist change was needed.
  - Tests: every package has `client_test.go` (httptest fake server per provider, asserting auth headers, endpoint paths, request bodies, and error surfacing) and `target_test.go` (full happy-path Deploy through a fake server + fake docker Runner, missing-Runner/missing-Registry guard errors, and a surfaced-failure case per provider's terminal status). `cmd/acthur/deploy_test.go` gained `TestRunDeploy_RemoteTargets_RequireTokenEnvVars` (gate blocks fly/railway/render before any docker/API call when the token env var is unset) and `TestRunDeploy_DryRun_RemoteTargets_PlanMentionsTarget`. No live leg exists for any of the three (no accounts) — matching the Coolify precedent from #50/#52.
  - `go build ./... && go vet ./... && go test ./...` all green at handoff.
  - Deferred/out of scope: no live witness against real fly.io/Railway/Render accounts (impossible without credentials, per task instructions). Multi-owner Render tokens are not disambiguated (documented in the package doc). Railway/Render's git-repo-based build flows (Render's `render.yaml` blueprint, Railway's repo-connected `serviceCreate`) were not implemented — the image-based flow was chosen as the honest, uniform, buildable-without-a-hosted-git-remote option consistent with how Acthur projects aren't assumed to live in a specific git host. `acthur secrets`/env-value injection into the deployed service (vs. just the ${VAR} *names* the compose projection already extracts) is not implemented for any of the three remote targets — same as coolify, which also only pushes compose YAML with `${VAR}` references and relies on the target platform's own env configuration, not push-time secret injection.
- 2026-07-06 — worktree agent-a9fbf93fdc015442c (#65, Phase 9 ecosystem plugins) — Implemented all five plugins the issue names: feature-flags, admin, observability, security, https. All are go:fiber generators mirroring the auth/rbac/multitenancy shape (RegisterGenerator + embedded templates + a real OnEvent hook), registered via blank import in `cmd/acthur/commands.go`, each with a generator-output test suite plus a `TestGenerate_CompilesInsideScaffoldedGofiberProject` gold-standard test (scaffold go:fiber → generate → `go mod tidy && go build ./...` on the real result), plus one combined `TestRunAdd_EcosystemPlugins_RoundTrip` in `cmd/acthur/add_ecosystem_test.go` proving the full `acthur add <plugin>` CLI flow for each.
  1. `feature-flags` — generates `internal/flags/flags.go`: a typed client reading the `ACTHUR_FLAG_<NAME>` env vars the dev engine already exposes (built in #62), plus a `Require(name)` fiber middleware gating a route behind a flag. Hook: `OnEvent(EventBeforeNodeStart)` logs how many project flags are enabled, read live from `.acthur/flags.json` via the CLI-side `internal/flags` store (same module, no generated-code coupling).
  2. `security` — generates `internal/security/security.go` wiring fiber's own `helmet`/`cors`/`limiter` middleware (all already reachable from the go:fiber scaffold's existing fiber dependency — zero new deps). The CORS allowlist is derived from **real** `data_flow` edges into the target node via a new `ctx.Extra["graph"]` convention (added to `cmd/acthur/add.go`, mirroring the existing `module_path` convention) — falls back to plugin config, then to a safe empty (deny-all-cross-origin) default, never a wildcard, matching PRD §18.2 literally.
  3. `observability` — generates `internal/observability/observability.go`: real Prometheus counters + a duration histogram per request, served at `GET /metrics` via fiber's `adaptor` middleware (`prometheus/client_golang` — a new dep in generated projects only, confirmed `go mod tidy` resolves it fine in this sandbox).
  4. `admin` — generates `internal/admin/admin.go`: a read-only dashboard (`GET /admin` HTML + `/admin/api/status` + `/admin/api/flags` JSON) over what Acthur can safely introspect today (service identity/uptime, this process's feature-flag state via `ACTHUR_FLAG_*` env).
  5. `https` — new `internal/plugin/builtin/https/certs.go`: generates a self-signed dev CA + leaf cert (SANs: dev domain, `*.<domain>`, localhost, 127.0.0.1) directly via `crypto/x509`, persisted under `.acthur/certs/`, reused while still valid (idempotent). `internal/proxy` gained a `WithTLS(certFile, keyFile)` option (switches `Start` to `ListenAndServeTLS`) and `internal/engine`'s `DevEngine` a matching `WithTLS` option threading it through — `cmd/acthur`'s `dev` command now honors the pre-existing `dev.https` config flag, calling `EnsureDevCert` and wiring the result in. Proved end-to-end with a real TLS handshake test (`internal/proxy/proxy_tls_test.go`), not just cert parsing.
  - Small shared-plumbing additions, all backward compatible: `internal/engine/dev.go`'s event emission now includes `root_dir` in `EventPayload.Data` (lets any plugin hook reach project-scoped state); `cmd/acthur/add.go`'s `GeneratorContext.Extra` now also carries `"graph"` (the live `*graph.Graph`); `internal/generate/engine.go`'s `lockKey` now also treats `.acthur/`-prefixed paths as project-root-scoped (alongside the pre-existing `migrations/`/`deploy/` cases), since a dev cert's README isn't per-service.
  - Deferred/explicitly descoped (documented in each plugin's package doc comment, not silent): `admin`'s full CRUD data-model editor over arbitrary user database models (PRD's fuller ambition — Acthur has no generic ORM/model-registry to introspect against yet); `observability`'s distributed tracing (Jaeger/Tempo), Loki logs pillar, and generated Grafana dashboards (metrics-only implemented for real); `security`'s SQL-injection-prevention/DTO-sanitization and auth/admin audit logging (need a generated repository layer and a log-sink contract that don't exist); `https`'s automatic OS/browser trust-store installation (mkcert-style — needs elevated, per-platform calls) and production TLS (Caddy/Nginx/ACME — that's deploy targets' job, Phase 8). None of the five plugins gained `SupportedAdapters()` beyond `go:fiber`, consistent with every prior plugin's precedent (chi/gin/rustaxum/fastify remain generator-agnostic by design, per #58/#59's own notes).
  - `go build ./... && go vet ./... && go test ./...` green at handoff (full repo, all packages, no `-short` skips — every new plugin's real compile test ran and passed, including a live network-dependent `go mod tidy` fetch of `prometheus/client_golang` for observability).
  - Not witnessed live against a running `acthur dev` process (no real `acthur new` + `acthur add <plugin>` + `acthur dev --https` end-to-end run in this worktree) — every claim above is proven at the unit/compile-test level (real generated code, real `go build`, a real TLS handshake in `proxy_tls_test.go`), consistent with how #62 (secrets/flags/monitor) was witnessed but this slice's time budget did not extend to a full live `acthur dev` session.

## Current Decisions
- Waves: (1) Phase 3+4+6/8 leftovers — three agents; (2) Phase 9 split across parallel agents. Merge + full suite + witness between waves.
- Conflict hotspot `cmd/acthur/commands.go`: each agent owns only its command block; orchestrator resolves import/status-note conflicts on merge.
- Anything genuinely unimplementable here (e.g. live VPS legs) is descoped explicitly in docs + tracker, never silently.

## Open Questions
- DNS auto-setup strategy (hosts file vs resolver) — wave 1 Phase 3 agent decides and records here.

## Acceptance Criteria
- [ ] No `notImplemented(3|4|5|6|8)` remains; each former stub has behavior tests
- [ ] `acthur dev` runs doctor preflight; watcher-driven reload cascade proven; service logs/restart/health work live
- [ ] Contract importers accept .openapi.yml/.proto/.graphql; proxy validates responses
- [ ] Phase 9 surfaces implemented (or explicitly descoped in docs): new/init, adapters, generate ai-context/ci/docs/skill, graph visualize, secrets/flag/monitor, mcp serve, agent, deploy targets, ecosystem plugins
- [ ] Full suite green; live witnesses for dev-runtime changes and new/init
