# Implementation: PRD Completion — audit-gap closure (Phases 3/4/6/8 leftovers + Phase 9)

## Goal
Close every gap `docs/audit.md` (2026-07-06) identified between the PRD and the implementation, in dependency order: Phase 3 integration (doctor-in-dev, watcher wiring, DNS scoping, service commands), Phase 4 (importers, response validation), Phase 6/8 leftovers (db subcommands, acthur test, acthur build), then Phase 9 (new/init wizard, adapters, generate surfaces, secrets/flag/monitor, mcp/agent, deploy targets, ecosystem plugins). Every stub `notImplemented(N)` either gains a real implementation or is explicitly descoped in docs with the PRD updated.

## Owning Docs
- `docs/audit.md` (gap inventory) · `docs/acthur-prd.md` §21 · tracker issue #53

## Status
In-flight
- 2026-07-06 — main — Audit triaged: Phase 8 findings were stale (deploy wired + witnessed in c9e41ce/824fe9c; suite green), rest confirmed. Stale tracked `acthur` binary removed. Wave 1 delegated to parallel Sonnet worktree agents.
- 2026-07-06 — worktree agent-a0808583ee16135c5 (#56) — Phase 6/8 db+build+test gaps closed: `acthur db migrate:create` (shares `migrations.CreateNext` numbering with `db create`), `acthur db seed` (+ `--file`, lexical `seeds/*.sql` order, via injectable `SeedExecutor`), `acthur db reset` (`--yes`-gated down-all+up-all+seed via injectable `ResetSteps`), `acthur db studio` (adminer container via the `deploy.Runner` seam, prints URL), `acthur test [service]` and `acthur build` (both reuse `buildableServiceNodes` from the deploy gate and the gate's new `deploy.GoStream` seam) were all already implemented by the prior agent but never committed. Root cause: an unanchored `acthur` line in `.gitignore` (from the binary-untracking cleanup) matched the `cmd/acthur/` directory itself, so `git add -A` silently excluded `build.go`/`db.go`/`test.go` from the WIP checkpoint commit. Fixed by anchoring the rule to `/acthur` (repo-root binary only) and recovered the three files. Added missing unit tests (build_test.go, test_test.go, db_test.go) covering node selection, failure aggregation, studio target parsing, and runner lifecycle. `go build ./... && go vet ./... && go test ./...` all green. No `notImplemented(6)`/`notImplemented(8)` remains for these five surfaces. Phase 3/4/9 items in this doc are out of this slice's scope and untouched.
- 2026-07-06 — worktree agent-aa02558092e19d708 (issue #55, Phase 4 gaps) — Completed both items:
  1. Contract importers: salvaged and finished the in-flight `internal/contract/import_openapi.go` (fixed a missing `plain bool` arg at 3 call sites, otherwise didn't compile). Added `internal/contract/import_proto.go` (hand-rolled proto3 parser: messages → Types, service rpcs → Endpoints with flattened request/response fields) and `internal/contract/import_graphql.go` (hand-rolled GraphQL SDL parser: object types → Types, enums tracked for `enum(...)` field rendering, Query/Mutation/Subscription fields → Endpoints). No new deps — stdlib + existing `gopkg.in/yaml.v3` only. Fixtures under `testdata/contract-import/{openapi,proto,graphql}/`. Replaced the old "not yet supported" tests in `internal/contract/contract_test.go` with real round-trip import assertions. Lossy/conventional mappings (GraphQL list returns → `{"items": "array($Type)"}`, gRPC endpoints have no Method/Path in the HTTP sense) are documented in code comments in the importer files.
  2. Proxy response validation: `internal/proxy/proxy.go` now validates flow-route backend responses against the contract's Output schema via the existing `contract.Validator.ValidateResponse` (already present, just unused by the proxy). Matched contract/endpoint is threaded from request-time enforcement to `ModifyResponse` via request context (`flowCheckCtxKey`). Dev mode logs a warning and forwards unchanged; `--strict` rewrites the response into a structured 502 (`{"error":{"code":"response_contract_violation",...}}`) before it reaches the client — including fixing the stale `Content-Length` header that would otherwise truncate/hang the rewritten body. `cmd/acthur/commands.go` dev command help text and `--strict` flag description updated to mention 502 response blocking. Tests added to `internal/proxy/flow_test.go` mirroring the existing request-validation test shape.
  - Also fixed an unrelated but critical bug found mid-task per parent-session heads-up: `.gitignore`'s unanchored `acthur` line was silently excluding all new files under `cmd/acthur/` from `git add -A` (this is how the prior agent on this issue lost work) — anchored to `/acthur`.
  - Full suite (`go build ./... && go vet ./... && go test ./...`) green at handoff.
  - Deferred/out of scope: nothing from issue #55's stated scope. Not addressed here (belongs to other phases per audit): Phase 3 doctor/DNS/watcher integration, Phase 6/8/9 gaps.
- 2026-07-06 — worktree agent-aaad08f6a74ac851d (#54) — Phase 3 gaps closed:
  doctor preflight in `acthur dev` + `--skip-doctor` (salvaged from prior WIP,
  verified green); `internal/dns` DNS preflight for `*.acthur.local`-style dev
  domains (`--write-hosts` opt-in, never runs sudo); watcher wired into
  `DevEngine` via a new `adapter.SelfReloader` capability so file changes
  restart a node's process unless its adapter (e.g. go:fiber's air)
  self-reloads; and `acthur service add/logs/restart/health` implemented —
  logs/pidfiles persisted under `.acthur/` by the dev engine
  (`internal/process/control.go`), `restart` signals the recorded pid
  (SIGTERM, relying on the existing Supervisor auto-restart-on-crash path —
  no new daemon/RPC), `health` probes directly, `add` mirrors `add.go`'s
  YAML-edit-with-rollback pattern. All four gaps are unit-tested (fake
  process manager / fake DNS lookup / fake health poller — no real
  processes or network); `go build ./... && go vet ./... && go test ./...`
  green. Also fixed an unrelated repo-wide bug found while resuming: an
  unanchored `acthur` line in `.gitignore` was silently excluding new files
  under `cmd/acthur/` from `git add -A` (root-caused the prior agent's lost
  `dev_preflight_test.go`) — anchored to `/acthur` and recovered the file.
  Needs a live witness: this closes the gaps at the unit-test level only —
  no run against a real `acthur dev` process (real file-change restart,
  real DNS resolution, real `acthur service restart` against a live pid)
  has been performed in this worktree.

- 2026-07-06 — worktree agent-aa30aa5cfbf4bf9de (issue #58, Phase 9 go:chi + go:gin) — Added two new backend adapters at Scaffold/Run/Dockerize parity with go:fiber: `internal/adapter/backend/chi` (net/http + go-chi/chi/v5 + go-chi/cors) and `internal/adapter/backend/gin` (gin-gonic/gin + gin-contrib/cors). Each scaffolds the same project shape as go:fiber — `/health` + `/api/health` with the identical JSON contract, a zap structured-logger middleware, config/errors/ids packages (ULID or UUID-v4 per `IDStrategy`), air-based hot reload (`SelfReloads() == true`, same `.air.toml` rerun/rerun_delay behavior as the #33 go:fiber fix), and a `dockerfile.prod.tmpl` mirrored from go:fiber's hard-won details verbatim (build root package `.`, not `./...`; healthcheck against `127.0.0.1`, never `localhost`; EXPOSE/HEALTHCHECK omitted for portless nodes; non-root `acthur` user). Both register via blank import in `cmd/acthur/commands.go` next to go:fiber, so `acthur adapter list` picks them up automatically (verified live: both show `[Scaffold Run Dockerize]`). Framework-idiomatic adaptations where fiber's API has no direct net/http/gin equivalent: chi has no centralized `ErrorHandler`, so `internal/errors.Write(w, err)` is the per-handler seam; gin gets an `errors.Handler()` middleware that reads `c.Errors` after `c.Next()` since gin's error model is accumulate-then-inspect rather than return-a-value. `internal/adapter/adapter_test.go` extended with a parametrized `backendAdapterCases()` suite covering both adapters in parallel with the existing go:fiber tests (registration, Detect true/false/no-go.mod, Runnable+SelfReloader, Scaffold file set + health routes + air rerun config, module-path consistency, ID strategy selection, Dockerizable multi-stage/non-root/EXPOSE/HEALTHCHECK/portless/deterministic, exact capability set {Scaffold, Run, Dockerize}), plus `TestBackendAdapters_Scaffold_CompilesWithGoBuild`: scaffolds each into a temp dir and runs `go mod tidy && go build ./... && go vet ./...` — both generated go:chi and go:gin projects compile and vet clean. `go build ./... && go vet ./... && go test ./...` green across the whole repo. Not in scope for this slice (per issue #58's stated scope, and confirmed against `cmd/acthur/add.go` / `internal/generate`): the contract-driven code generator (`acthur add`, `acthur generate from-contract`) and the auth/rbac/multitenancy plugin generators remain go:fiber-only (`SupportedAdapters() []string{"go:fiber"}`) — extending those to chi/gin is a separate, larger effort not implied by this issue's "Scaffold/Run/Connectable/Dockerize parity" wording (which lists the adapter capability set, not that go:fiber itself implements Connectable — it doesn't, and neither do chi/gin).

- 2026-07-06 — main — Wave 1 merged to dev (all three branches), full suite green. Live witness (scratchpad witness-w1, gofiber api + postgres db): `acthur build` → static binary at `.acthur/build/api`; `acthur test` green; doctor preflight correctly aborted dev on missing tools (and the witness caught a real bug: `cmdVersion` used `cmd.Output()` but air prints its version to stderr — doctor reported installed air as missing; fixed with `CombinedOutput` + regression test); DNS preflight printed copy-pastable /etc/hosts instructions; `.acthur/logs/<node>.log` + `.acthur/run/<node>.pid` written by the engine; `service health` ✓, `service logs` tails live air/request output, `service restart` bounced the app (uptime reset, healthy after); file-change hot reload proven both ways — broken file → air build failure + shutdown + supervisor recovery, fixed file → rebuild → healthy, with zero engine-driven restarts (SelfReloader honored). Wave 1 acceptance criteria met; Wave 2 (Phase 9, #57–#65) launching.
- 2026-07-06 — worktree agent-abe2e6029e4ffa560 (#57, Phase 9 slice) — Completed all three gaps:
  1. `acthur new <project>` — non-interactive (flag-driven: `--adapter`, `--db`, `--module`) and interactive (injectable `io.Reader`/`io.Writer` wizard, prompts only for answers not already set via flags) paths share `resolveNewOptions` (`cmd/acthur/new.go`). Non-TTY stdin with any answer missing is a pointed error naming exactly which flags to pass — never a hang on an unsatisfiable read (detected via `github.com/mattn/go-isatty`, already an indirect dep, promoted to direct via `go mod tidy`). `scaffoldProject` writes a minimal valid `acthur.yml` (one `api` service node on the chosen backend adapter, optional `db` infra node on `db:postgres`, `api→proxy proxied_through` edge always present so the always-materialized kernel proxy node satisfies the no-orphan-node rule, `api→db depends_on` when `--db`), scaffolds the adapter's files under `<project>/api/` via its `Scaffolder.Scaffold` (same `scaffold.ResolveScaffoldContext` used by `acthur add`), and runs `git init` (best-effort — reported via `scaffoldResult.GitInit`, never fails the command). Adapter choice is validated against `adapter.ByCategory(CategoryBackend)` adapters that implement `Scaffolder` — picking e.g. `db:postgres` (registered but not a Scaffolder) is a pointed error.
  2. `acthur init` — same wizard/scaffold path targeting cwd instead of a fresh subdirectory; shares `scaffoldProject`'s existing-`acthur.yml` guard for the non-destructive guarantee.
  3. `acthur plugin remove <name>` — `cmd/acthur/plugin_remove.go` mirrors `add.go`'s YAML-edit-with-rollback pattern: line-based removal of the `  - name: <name>` entry and its nested fields (indented 4+ spaces, e.g. a `config:` block) from acthur.yml's `plugins:` list, reload + `graph.Build` to confirm the file is still valid (rolling back on any failure), pointed "not installed" error listing every currently-installed plugin name when the target isn't present. Fixed the same trailing-newline-loss bug `service add` had already been patched for (removing the last entry in the file could drop the file's final newline) — regression-tested.
  Live-witnessed with a built binary (not `go run`, since a fresh dev dir has no go.mod): `acthur new demo --adapter go:fiber --db --module github.com/acme/demo` → real `acthur.yml` + `demo/api/{main.go,go.mod,Dockerfile,...}` + `.git/`, and `acthur graph validate` against the generated project reports "all 3 nodes and 2 edges are valid" with zero errors; `acthur init` into an existing dir refuses a second run once `acthur.yml` exists; `acthur add migrations` → `acthur plugin remove migrations` round-tripped cleanly, second `remove` call correctly points at "no plugins listed"; `.gitignore`'s `/acthur` anchoring (fixed by prior agents) confirmed working — `git status` shows all four new files (`new.go`, `new_test.go`, `plugin_remove.go`, `plugin_remove_test.go`) as untracked, not silently excluded.
  Deferred/out of scope (per issue #57's stated scope — full interactive wizard steps like framework choice beyond adapter, plugin selection, deployment target selection, and `acthur init`'s stack-detection/adoption-of-existing-code scan are broader Phase 9 PRD ambitions not asked for here; only `go:fiber` is a registered backend Scaffolder today so the adapter prompt has exactly one real choice): `acthur init`'s file-detection/reconciliation phases (§7.2 Init Phases 1/4 "Scan"/"Reconcile" in the PRD) are not implemented — it only ever writes a fresh scaffold, it does not detect an existing non-Acthur codebase and adopt it non-destructively around existing files. `go build ./... && go vet ./... && go test ./...` all green at handoff.
- 2026-07-06 — worktree agent-a625fae97484cea44 (#59, Phase 9) — Backend adapter parity: added `rust:axum` and `node:fastify`, same capability shape as `go:fiber` (Scaffolder, Runnable, SelfReloader, Dockerizable — neither is Containerized, matching go:fiber's precedent of building from project source rather than an upstream image). Both scaffolds share go:fiber's behavioral contract: `/health` + `/api/health` (dev-proxy-prefixed) returning the identical `{status, service, uptime_seconds, checks}` JSON shape, structured request logging (tower-http `TraceLayer`/tracing for axum, Fastify's built-in pino logger), config from env with the same required-var set (APP_ENV/APP_PORT/APP_SECRET[generate]/DATABASE_URL/REDIS_URL), CORS + compression middleware, graceful shutdown on SIGTERM/SIGINT, ULID/UUID `IDStrategy` branching (axum: `ulid`/`uuid` crates; fastify: hand-rolled Crockford-base32 ULID — no external dep — plus `crypto.randomUUID`). Hot reload: `cargo-watch -x run` for axum, `node --watch src/index.js` for fastify — both self-restart the process themselves, so `SelfReloads() → true` on both (dev engine's file watcher defers to them, same as go:fiber's air). Dockerfile capability: scaffold-time `Dockerfile` (no healthcheck, hardcoded :8080, mirrors go:fiber's) plus `DockerfileFor` (Dockerizable, re-rendered per `acthur deploy`) — both multi-stage (rust:1.82-alpine musl builder → alpine:3.20 runtime; node:24-alpine builder → node:24-alpine runtime), non-root user, HEALTHCHECK against **127.0.0.1** (never `localhost` — confirmed alpine/musl resolves it to `::1`, same hard-won lesson as go:fiber's dockerfile.prod.tmpl), EXPOSE/HEALTHCHECK omitted when `ctx.Port == 0` (portless/worker nodes). Registered both in `cmd/acthur/commands.go` alongside go:fiber/db:postgres — `internal/deploy/artifacts` needed no changes since it projects any `Dockerizable` adapter generically via `adapter.Resolve` + type assertion; `acthur adapter list` confirms both show up with `[Scaffold Run Dockerize]`. Existing generator plugins (auth/rbac/multitenancy) remain go:fiber-only by explicit design (`SupportedAdapters()` gate) — out of this issue's scope, not a gap.
  - Verification: rebuilt this worktree onto current `dev` first (it had drifted ~40 commits stale, predating `docs/audit.md`, the Dockerizable capability, and issue #53-56's work — clean rebase, no conflicts). `cargo check` passes on a rendered rust:axum scaffold (13s cold, network access to crates.io confirmed available in this sandbox despite `curl` to crates.io returning 403 — cargo's own client works fine). Every rendered node:fastify `.js` file passes `node --check`; `package.json` parses as valid JSON with `fastify` declared and dev/build/test scripts present (real `npm install` skipped per scope — no dependency-shape or type-checking issue expected, plain JS was chosen specifically to avoid needing a TS compile step here).
  - Test coverage per adapter (`internal/adapter/backend/{rustaxum,fastify}/*_test.go`, ~30 tests each): registry/name/category/detect, env vars, Runnable commands + SelfReloader, Scaffold file production/content/ID-strategy branching, Dockerizable multi-stage/non-root/port-EXPOSE/127.0.0.1-healthcheck/no-port-omission/determinism, exact `CapabilitiesOf` set, plus the compile/syntax-check tracer-bullet test (gated behind `testing.Short()` like go:fiber's).
  - `go build ./... && go vet ./... && go test ./...` green (full repo, all packages) at handoff.
  - Deferred/out of scope: no live witness of `acthur dev` actually running a scaffolded rust:axum or node:fastify service end-to-end (no `cargo run`/full `npm install` performed — network-heavy and out of this slice's verification scope per task instructions). Generator plugin parity (auth/rbac/multitenancy for the new adapters) not attempted — those are explicitly go:fiber-only by design today and would be a separate, larger effort.
- 2026-07-06 — worktree agent-abe6004a9981b4288 (#60, Phase 9 frontend adapters) — Added `ui:astro` and `ui:next` at `internal/adapter/frontend/{astro,next}`, both `adapter.CategoryFrontend`, registered in `cmd/acthur/commands.go` (`acthur adapter list` now shows a `frontend` category, both `[Scaffold Run Dockerize]` — same capability shape as every backend adapter; neither is Connectable/Containerized, matching precedent). Key design decision: the kernel's health checker treats every service node uniformly (`internal/health/health.go` `strategyFor`: `node.IsService()` → HTTP GET /health, no category branch), so both frontends had to be real SSR/server apps with a live `/health` endpoint, not static sites — `ui:astro` uses `output: 'server'` + `@astrojs/node` in standalone mode (`src/pages/health.js` endpoint, `prerender = false`), `ui:next` uses the App Router (`app/health/route.ts`, Node.js runtime by default) with `output: 'standalone'` for a self-contained production server. Both emit the identical `{status, service, uptime_seconds, checks}` JSON body every backend adapter uses, so the kernel's health poller stays framework-agnostic. Port handling: the dev engine injects `PORT`/`APP_PORT` into the adapter's env before spawning it (`internal/engine/dev.go` `buildEnv`) — astro.config.mjs reads `process.env.PORT` directly (also the var @astrojs/node's own standalone runtime reads, so dev and prod agree); ui:next's `DevCommand` reads `APP_PORT`/`PORT` from the env map passed in and threads it as an explicit `next dev -- -p <port>` CLI arg for version-independent certainty. Both self-reload (`SelfReloads() → true`) via their own dev servers (astro's Vite-powered watcher, Next's Fast Refresh) — the dev engine's file watcher defers to them, same as every other adapter. Dockerfiles: multi-stage `node:24-alpine`, non-root `USER acthur`, `HEALTHCHECK` against `127.0.0.1` (never `localhost` — same hard-won alpine/musl lesson as every other adapter), `EXPOSE`/`HEALTHCHECK` omitted for portless nodes. `pathPrefixFor` in `internal/proxy/proxy.go` already special-cased `"web"`/`"frontend"`/`"app"`/`"www"` node IDs to the root `/` prefix (pre-existing, comment already mentioned "Next.js HMR") — no proxy changes needed, and no `/api/health` duplicate route was needed (that pattern in gofiber/chi/gin exists specifically for API services proxied under a non-root `/api-style` prefix; a root-mounted frontend doesn't need it).
  - Verification (all real, not mocked): rebuilt this worktree onto current `dev` first (brought in go:chi/go:gin/rust:axum/node:fastify/acthur-new-init and the backendAdapterCases suite). Per-adapter Go test suites (`internal/adapter/frontend/{astro,next}/*_test.go`, ~30 tests each, mirroring node:fastify's shape) cover registration/name/category, Detect true/false/no-package.json, EnvVars (present, no DATABASE_URL/APP_SECRET — frontends aren't backends), Runnable+SelfReloader, Scaffold file set + package.json validity + framework-specific config assertions (astro: `output: 'server'`, reads `process.env.PORT`; next: `output: 'standalone'`), health-endpoint contract assertions, Dockerizable multi-stage/non-root/port-EXPOSE/127.0.0.1-healthcheck/no-port-omission/determinism, exact `CapabilitiesOf` set. Beyond unit tests, did a full live witness for both, time-boxed but real: scaffolded each into `/tmp/.../scratchpad/witness-{astro,next}`, ran real `npm install` (349 packages for astro in ~11s, 28 for next in ~10s — network confirmed available) and real `npm run build` (astro: `astro build` produced `dist/server/entry.mjs`; next: `next build` — including full TypeScript type-check of `layout.tsx`/`page.tsx`/`health/route.ts` — produced `.next/standalone/server.js`), then **ran the actual built servers** (`node dist/server/entry.mjs` / `node .next/standalone/server.js`) with `PORT` env injected exactly as the dev engine would, and `curl`'d `/health`: both returned HTTP 200 with `{"status":"healthy","service":"web","uptime_seconds":...,"checks":{}}`, and `/` served 200. This is the strongest verification available short of wiring a live `acthur dev` run (not attempted — `acthur new`/`add` don't yet offer a frontend-adapter prompt path, an `acthur new` UX gap, not an adapter-contract gap; out of this issue's scope). Every generated `.js`/`.mjs` file passed `node --check`; `package.json`/`tsconfig.json` parsed as valid JSON. `go build ./... && go vet ./... && go test ./...` green across the whole repo at handoff.
  - Deferred/out of scope: no wiring of `ui:astro`/`ui:next` into `acthur new`'s adapter-choice prompt (today only scans `adapter.ByCategory(CategoryBackend)` Scaffolders — a frontend option there is a separate, `new.go`-level UX change, not part of this issue's "scaffold + dev-run wiring through the graph/proxy" scope). No generator-plugin parity (auth/rbac/multitenancy remain go:fiber-only by explicit design, same precedent as #58/#59). No live `acthur dev` process run against either adapter (see verification note above for why the strongest available alternative was performed instead).

## Current Decisions
- Waves: (1) Phase 3+4+6/8 leftovers — three agents; (2) Phase 9 split across parallel agents. Merge + full suite + witness between waves.
- Conflict hotspot `cmd/acthur/commands.go`: each agent owns only its command block; orchestrator resolves import/status-note conflicts on merge.
- Anything genuinely unimplementable here (e.g. live VPS legs) is descoped explicitly in docs + tracker, never silently.

## Open Questions
- DNS auto-setup strategy (hosts file vs resolver) — wave 1 Phase 3 agent decides and records here.

## Acceptance Criteria
- [ ] No `notImplemented(3|4|5|6|8)` remains; each former stub has behavior tests
- [ ] `acthur dev` runs doctor preflight; watcher-driven reload cascade proven; service logs/restart/health work live
- [ ] Contract importers accept .openapi.yml/.proto/.graphql; proxy validates responses
- [ ] Phase 9 surfaces implemented (or explicitly descoped in docs): new/init, adapters, generate ai-context/ci/docs/skill, graph visualize, secrets/flag/monitor, mcp serve, agent, deploy targets, ecosystem plugins
- [ ] Full suite green; live witnesses for dev-runtime changes and new/init
